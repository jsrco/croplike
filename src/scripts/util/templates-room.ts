import { RoomMap } from "./create-room"

export const demoRoom: RoomMap = {
        entityCountMap: {
            bigDemoEntity: 1,
            demoEntity: 1,
            growthDemoEntity: 1,
        },
        systemMap: {
            movement: true,
            physics: true,
            render: true, 
        }
}

