export type EntityMap = {
    name: string
    componentMap: {
        collision: boolean
        graphics: boolean
        gravity: boolean
        jump: boolean
        position: boolean
        size: boolean
        velocity: boolean
        wallCollision: boolean
        wall: boolean
    }
}